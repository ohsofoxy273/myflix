%article.video_category
	%header
		%h3= @category.name.titleize
		- @category.videos.in_groups_of 6 do |group| 
			.videos.row
			%br
			- group.each do |video|
				- unless video.nil?
					.video.col-sm-2
						= link_to image_tag(video.small_cover_url), video_path(video.id)